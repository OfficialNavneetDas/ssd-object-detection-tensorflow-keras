# ssd-object-detection-tensorflow-keras
Single Shot MultiBox Detector (SSD) object detection implemented using TensorFlow and Keras with a custom training loop, avoiding the TensorFlow Object Detection API. Includes TFRecord parsing, data pipeline, anchor-based predictions, and end-to-end training.
